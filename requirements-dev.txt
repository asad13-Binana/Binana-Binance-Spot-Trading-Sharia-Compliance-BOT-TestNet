# DEP-HASH-001: development and CI tooling only. Runtime dependencies are in
# the two fully hash-locked files and must be installed separately with
# --require-hashes. Mixing hashed runtime pins and unhashed tooling in one pip
# invocation would make the tooling subject to hash-checking and fail.
pytest==9.0.2
PyYAML==6.0.2
pip-audit==2.9.0
hypothesis==6.165.5
coverage==7.15.4
bandit==1.9.4
ruff==0.16.2
